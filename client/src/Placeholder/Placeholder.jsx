/**
 * Random placeholder component, useful maybe for setting up layouts and testing?
 * 
 * @author Ryan Letto
 */
 import React from 'React';

// It's literally just a heading with a message
const Placeholder = (msg) => (
    <h1>
        {msg}
    </h1>
);

export default Placeholder;
